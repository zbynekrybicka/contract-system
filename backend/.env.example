APP_ENV=dev
APP_SECRET=change-me

# Docker DEV DB (host = služba "mysql" z docker-compose)
DATABASE_URL="mysql://appuser:apppass@mysql:3306/contract_system?serverVersion=8.0.31&charset=utf8mb4"

# JWT (cesty neměň; doplň si jen passphrase v .env.local)
JWT_SECRET_KEY=%kernel.project_dir%/config/jwt/private.pem
JWT_PUBLIC_KEY=%kernel.project_dir%/config/jwt/public.pem
JWT_PASSPHRASE=heslo123
