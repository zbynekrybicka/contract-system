<?php
namespace App\Tests\Controller;

use Symfony\Bundle\FrameworkBundle\Test\WebTestCase;

final class %RequestTitle%Test extends WebTestCase {

    /**
     * @dataProvider data%RequestTitle%
     */
    public function test%RequestTitle%(): void
    {
        // HTTP client
        $client = static::createClient();

        // Data
        $data = [
        ];

        // HTTP Request
        $client->request("%Method%", "/%URL%", [], [], ['CONTENT_TYPE' => 'application/json'], json_encode($data));
        
        // Response control
        // $this->assertContains($client->getResponse()->getStatusCode(), [401]);
    }

    public static function data%RequestTitle%(): array 
    {
        return [

        ];
    }

}