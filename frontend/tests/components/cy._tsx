import { Provider } from 'react-redux'
import { store }  from '../../src/store'
import %ComponentName% from '../../src/components/%ComponentName%'


describe('%ComponentName%.cy.tsx', () => {
  it('%testName%', () => {
    cy.mount(<Provider store={store}><%ComponentName% /></Provider>)
  })
})